
// No special repository methods needed yet for user management
// as the service layer can directly use the Supabase client for admin actions.
// This file is created for consistency and future expansion.

class UserManagementRepository {
    // Future methods can be added here, for example:
    // async getRecentlyDisabledUsers() { ... }
}

export const userManagementRepository = new UserManagementRepository();
